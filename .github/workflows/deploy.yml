name: Deploy to Hostinger

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 191.101.230.15
          username: u502587017.erp.jbmpschool.in
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          server-dir: /htdocs/erp.jbmpschool.in/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.env
            **/backups/**
            **/*.log
